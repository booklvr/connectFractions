const { createGlobalStyle } = require('styled-components')

export const GlobalStyle = createGlobalStyle`

:root{
    --cellWidth: 50px;
    --white: white;
    --blue: blue;
    --yellow: yellow;
 }
`
