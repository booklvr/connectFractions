import styled from 'styled-components/macro'

export const UndoButtonContainer = styled.button`
  cursor: pointer;
  width: 10rem;
  height: 2rem;
  transition: all 0.1s ease;

  &:hover {
    transform: scale(1.1);
  }
`
